"""
Initialize the tests package for the Gemini Chat CLI.
"""

from pathlib import Path
import sys

# Add the parent directory to path to allow imports
sys.path.append(str(Path(__file__).parent.parent))
