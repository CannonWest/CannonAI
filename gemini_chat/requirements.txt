google-genai>=0.5.0
tabulate>=0.9.0
colorama>=0.4.4  # For cross-platform colored terminal output


starlette>=0.27.0 # Usually a dependency of FastAPI
python-multipart>=0.0.6 # For file uploads, if needed by UI

pytest>=7.0.0    # For general testing
pytest-asyncio>=0.18.0 # For testing asyncio code
pytest-mock>=3.5.0 # For easy mocking with pytest

coverage>=6.0 # For code coverage reporting (optional but recommended)
playwright>=1.20 # For Web UI E2E testing (optional)
selenium>=4.0 # Alternative for Web UI E2E testing (optional)